import { GreenTextDirective } from './green-text.directive';

describe('GreenTextDirective', () => {
  it('should create an instance', () => {
    const directive = new GreenTextDirective();
    expect(directive).toBeTruthy();
  });
});
